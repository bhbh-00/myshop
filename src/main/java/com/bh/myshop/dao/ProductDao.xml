<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bh.myshop.dao.ProductDao">

	<select id="getproductByReply" resultType="Product">
		SELECT P.id
		FROM
		product AS P
		LEFT JOIN
		reply AS R
		ON P.id = R.relId
		WHERE R.id = #{id}
		AND
		P.delStatus = 0;
	</select>

	<!-- 가장 최신 categoryId=2 2개 -->
	<select id="getLatestproductByBoardNameFree"
		resultType="Product">
		SELECT P.*,
		IFNULL(M.nickname, "탈퇴회원") AS extra__writer,
		C.name AS extra__categoryName
		FROM product AS P
		LEFT JOIN `member` AS M
		ON
		P.memberId = M.id
		LEFT JOIN category AS C
		ON P.categoryId = C.id
		WHERE
		P.categoryId = 2
		AND P.delStatus = 0
		ORDER BY P.id DESC
		LIMIT 2;
	</select>

	<!-- 가장 최신 categoryId=1 2개 -->
	<select id="getLatestproductByBoardNameNotice"
		resultType="Product">
		SELECT P.*,
		IFNULL(M.nickname, "탈퇴회원") AS extra__writer,
		C.name AS extra__categoryName
		FROM product AS P
		LEFT JOIN `member` AS M
		ON
		P.memberId = M.id
		LEFT JOIN category AS C
		ON P.categoryId = C.id
		WHERE
		P.categoryId = 1
		AND P.delStatus = 0
		ORDER BY P.id DESC
		LIMIT 2;
	</select>

	<select id="getForPrintProductByMemberId" resultType="Product">
		SELECT P.*,
		IFNULL(M.nickname, "탈퇴회원") AS extra__writer,
		C.name AS
		extra__categoryName
		FROM product AS P
		LEFT JOIN `member` AS M
		ON
		P.memberId = M.id
		LEFT JOIN
		category AS C
		ON P.categoryId = C.id
		WHERE
		P.memberId = #{id}
		AND P.delStatus
		= 0;
	</select>

	<select id="getproductsTotleCountByMyList" resultType="int">
		SELECT count(*)
		FROM product
		WHERE memberId = #{id}
		AND categoryId =
		#{
		categoryId }
		AND delStatus = 0
		<if test="searchKeywordType == 'name'">
			AND name LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="searchKeywordType == 'body'">
			AND `body` LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="searchKeywordType == 'color'">
			AND color LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="searchKeywordType == 'price'">
			AND price LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="searchKeywordType == 'fee'">
			AND fee LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="searchKeywordType == 'nameAndBodyAndColorAndPriceAndFee'">
			AND (name LIKE CONCAT('%', #{searchKeyword}, '%') OR
			`body` LIKE CONCAT('%', #{searchKeyword}, '%'))
		</if>
		ORDER BY id DESC
	</select>

	<select id="getproductsTotleCount" resultType="int">
		SELECT count(*)
		FROM product
		WHERE 1
		AND categoryId = #{categoryId}
		<if test="searchKeywordType == 'name'">
			AND name LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="searchKeywordType == 'body'">
			AND `body` LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="searchKeywordType == 'color'">
			AND color LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="searchKeywordType == 'price'">
			AND price LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="searchKeywordType == 'fee'">
			AND fee LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="searchKeywordType == 'nameAndBodyAndColorAndPriceAndFee'">
			AND (name LIKE CONCAT('%', #{searchKeyword}, '%') OR
			`body` LIKE CONCAT('%', #{searchKeyword}, '%') OR
			color LIKE
			CONCAT('%', #{searchKeyword}, '%') OR
			price LIKE CONCAT('%',
			#{searchKeyword}, '%') OR
			fee LIKE CONCAT('%', #{searchKeyword}, '%'))
		</if>
		AND delStatus = 0
		ORDER BY id DESC
	</select>

	<select id="getForPrintproducts" resultType="Product">
		SELECT P.*,
		IFNULL(M.nickname, "탈퇴회원") AS extra__writer,
		C.name AS
		extra__categoryName
		FROM product AS P
		LEFT JOIN `member` AS M
		ON
		P.memberId
		= M.id
		LEFT JOIN category AS C
		ON P.categoryId = C.id
		WHERE 1
		AND P.delStatus
		= 0
		<if test="categoryId != null">
			AND P.categoryId = #{categoryId}
		</if>
		<if test="searchKeywordType == 'name'">
			AND P.name LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="searchKeywordType == 'body'">
			AND P.`body` LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="searchKeywordType == 'color'">
			AND P.color LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="searchKeywordType == 'price'">
			AND P.price LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="searchKeywordType == 'fee'">
			AND P.fee LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="searchKeywordType == 'nameAndBodyAndColorAndPriceAndFee'">
			AND (P.name LIKE CONCAT('%', #{searchKeyword}, '%') OR
			P.`body` LIKE CONCAT('%', #{searchKeyword}, '%')OR
			P.color LIKE
			CONCAT('%', #{searchKeyword}, '%')OR
			P.price LIKE CONCAT('%',
			#{searchKeyword}, '%')OR
			P.fee LIKE CONCAT('%', #{searchKeyword},
			'%'))
		</if>
		<!-- 페이지 마이바티스 if and, or 검색해보기 -->
		ORDER BY P.id ASC
		<if test="limitStart != null and limitTake != null">
			LIMIT #{limitStart}, #{limitTake}
		</if>
	</select>

	<select id="getCategory" resultType="Category">
		SELECT *
		FROM category
		WHERE
		id = #{id}
		AND delStatus = 0;
	</select>

	<!-- 상품 상세페이지 -->
	<select id="getForPrintproduct" resultType="Product">
		SELECT P.*,
		IFNULL(M.nickname, "탈퇴회원") AS extra__writer
		FROM product AS P
		LEFT JOIN
		`member` AS M
		ON P.memberId = M.id
		WHERE P.id = #{id}
		AND P.delStatus = 0
	</select>

	<select id="getproduct" resultType="Product">
		SELECT *
		FROM product
		WHERE id
		= #{id}
		AND delStatus = 0
	</select>

	<select id="getproducts" resultType="Product">
		SELECT *
		FROM product
		WHERE 1
		AND delStatus = 0
		<if test="searchKeywordType == 'name'">
			AND name LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="searchKeywordType == 'body'">
			AND `body` LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="searchKeywordType == 'color'">
			AND color LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="searchKeywordType == 'price'">
			AND price LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="searchKeywordType == 'fee'">
			AND fee LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="searchKeywordType == 'nameAndBodyAndColorAndPriceAndFee'">
			AND (name LIKE CONCAT('%', #{searchKeyword}, '%') OR
			`body` LIKE CONCAT('%', #{searchKeyword}, '%') OR
			color LIKE
			CONCAT('%', #{searchKeyword}, '%') OR
			price LIKE CONCAT('%',
			#{searchKeyword}, '%') OR
			fee LIKE CONCAT('%', #{searchKeyword}, '%'))
		</if>
		ORDER BY id DESC
	</select>

	<!-- 상품 등록 -->
	<insert id="add" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO
		product
		SET regDate = NOW(),
		updateDate = NOW(),
		categoryId = #{categoryId},
		memberId = #{memberId},
		name = #{name},
		`body` = #{body},
		color = #{color},
		price = #{price},
		fee = #{fee}
	</insert>

	<!-- 상품 삭제 -->
	<update id="delete">
		UPDATE product
		SET delStatus = 1,
		delDate = NOW()
		WHERE
		id = #{id}
	</update>

	<!-- 상품 수정 -->
	<update id="modify">
		UPDATE product
		<set>
			<if test="name != null or body != null or color != null or price != null or fee != null">
				updateDate = NOW(),
			</if>
			<if test="name != null">
				name = #{name},
			</if>
			<if test="body != null">
				`body` = #{body},
			</if>
			<if test="color != null">
				color = #{color},
			</if>
			<if test="price != null">
				price = #{price},
			</if>
			<if test="fee != null">
				fee = #{fee},
			</if>
		</set>
		WHERE id = #{id}
		AND delStatus = 0
	</update>
	
	<!-- 기존의 상품명 확인 -->
	<select id="getProductByName" resultType="Product">
		SELECT *
		FROM product
		WHERE name = #{name}
	</select>

</mapper>